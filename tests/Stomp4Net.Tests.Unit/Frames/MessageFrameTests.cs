namespace Stomp4Net.Test.Frames
{
    using FluentAssertions;
    using Stomp4Net.Model;
    using Xunit;

    public class MessageFrameTests
    {
        [Fact]
        public void CheckCommand()
        {
            // var connectFrame = new MessageFrame();
            // connectFrame.Command.Should().Be(StompCommand.Message);
        }
    }
}
